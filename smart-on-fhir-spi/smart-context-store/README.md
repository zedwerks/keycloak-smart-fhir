# smart-context-spi

This is an SPI for the Basic SMART on FHIR EHR-Launch flows. This exposes protected endpoints 
for setting and clearing EHR context.

This depends on the smart-context-store-spi that is used to persist the context payload into a Dao.
