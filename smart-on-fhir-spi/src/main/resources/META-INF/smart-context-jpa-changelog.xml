<?xml version="1.0" encoding="UTF-8"?>
<!--
  Liquibase changelog for smart_context_entry table
  Copyright 2025 Zed Werks Inc.
  SPDX-License-Identifier: Apache-2.0
-->
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="smart-context-001" author="bhead">
        <createTable tableName="smart_context_entry">
            <column name="user_session_id" type="VARCHAR(64)">
                <constraints primaryKey="true" nullable="false" primaryKeyName="pk_smart_context_entry"/>
            </column>

            <column name="realm_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <!-- Store raw JSON payload as a CLOB/TEXT -->
            <column name="payload_json" type="CLOB">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="expire_at" type="TIMESTAMP"/>
        </createTable>

        <!-- Indexes for query performance -->
        <createIndex indexName="idx_smart_context_session"
                     tableName="smart_context_entry">
            <column name="user_session_id"/>
        </createIndex>

        <createIndex indexName="idx_smart_context_realm"
                     tableName="smart_context_entry">
            <column name="realm_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>