meta {
  name: Get SMART Context
  type: http
  seq: 4
}

get {
  url: {{keycloak_url_base}}/realms/smart/smart-on-fhir/context/{{context_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{$oauth2.credentials.access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
